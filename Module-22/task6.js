const array = ['shuvo', 'sabuj', 'supto', 'uthan', 'xxxx']

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    if (element.length % 2 !== 0) {
        console.log('Hi, barbie');

    }
}