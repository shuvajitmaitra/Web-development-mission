// Use of map ...................
const array = [2, 4, 6, 8, 10];

// function doubleIt(number) {
//     return number * 2;

// }
// const doubleIt = (number) => number * 2;

// const newArray = array.map(doubleIt);
const newArray = array.map((number) => number * 2);
console.log(newArray);