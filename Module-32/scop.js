function add(a, b) {
    if (b > 5) {
        const sum = a + b;
        const current = sum;
    }
    else if (b > 3) {
        const sum = a + b;
        const current = sum;
    }
    else {
        const sum = a + b;
        var current = sum;
    }
    console.log(current);
}

add(5, 3);
